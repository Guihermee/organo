import './banner.css'

const Banner = () => {
    // JSX
    return (
        <header className="banner">
            <img src="/img/banner.png" alt="Banner principal da página"></img>
        </header>
    );
}

export default Banner;
